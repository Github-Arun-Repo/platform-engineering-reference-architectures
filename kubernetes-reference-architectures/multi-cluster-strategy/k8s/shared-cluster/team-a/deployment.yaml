apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-a-demo
  namespace: team-a
  labels:
    app: team-a-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: team-a-demo
  template:
    metadata:
      labels:
        app: team-a-demo
    spec:
      serviceAccountName: team-a-app-sa
      containers:
        - name: web
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          env:
            - name: APP_TOKEN
              valueFrom:
                secretKeyRef:
                  name: team-a-app-secret
                  key: APP_TOKEN
            - name: APP_ENV
              valueFrom:
                secretKeyRef:
                  name: team-a-app-secret
                  key: APP_ENV
