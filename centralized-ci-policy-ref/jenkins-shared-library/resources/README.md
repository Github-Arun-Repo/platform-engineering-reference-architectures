This directory is intentionally non-authoritative for policy.

Central policy files are loaded directly from ../../central-policy at runtime in this reference architecture so there is a single source of truth.

If policy files are ever packaged here in future, treat them as generated artifacts only and add consistency tests.
